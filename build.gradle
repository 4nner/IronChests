// Aggregator only. Build logic lives in the module build files:
// - iron-chests/common: loader-agnostic code, no loader dependencies.
// - iron-chests/fabric: Fabric entrypoint, platform glue, resources.
